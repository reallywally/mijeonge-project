# 프론트엔드 개발 계획

캔버스에 그려 둔 화면을 실제 제품으로 옮기는 계획이다. 화면 스펙은 `../memo.md`(화면 목록 ·
시나리오)와 디자인 캔버스가 정답이고, 이 문서는 **무엇을 어떤 순서로 만들지**만 적는다.

- 제품명: **innoFlow** (옛 이름 "미정이"는 가제)
- 디자인 캔버스: <https://claude.ai/code/artifact/d16bbaab-0156-4403-8e68-3d565c132e0d>
  (페이지: 작업 시안 · 회의 시안 · 안건 시안)
- 캔버스와 코드의 역할 분담 — **구조·흐름이 바뀌면 캔버스 먼저, 문구·여백·색은 코드에서 바로**.
  둘 다 최신으로 맞추려 들면 어긋난다. 캔버스는 결정의 기록, 코드가 픽셀의 정답이다.

## 정해 둔 것

| | 결정 | 왜 |
| --- | --- | --- |
| 백엔드 붙이는 시점 | **작업(task) 화면까지 목업으로 만든 뒤** | 스키마에서 제일 무거운 작업·링크·기간이 화면에서 확정된 다음 계약을 잡아야 두 번 안 고친다 |
| 간트차트 | **dhtmlx-gantt v10 Community** | 캔버스 간트 메모가 이 라이브러리 기준으로 적혀 있다. JS 라이브러리라 Vue 래퍼는 직접 쓴다. Community는 GPL 계열이라 **사내 배포 전 라이선스 확인 필요** |
| 스택 | Vue 3 + Vite + TS + Tailwind v4 + shadcn-vue(new-york · neutral) + Pinia | 이미 서 있는 것을 이어간다 |
| 테마 | shadcn 기본값. 본문 폰트만 IBM Plex Sans KR | 캔버스의 종이 톤(#faf8f4 · 세리프 제목)은 코드로 옮기지 않는다 — 한 번 옮겼다 되돌렸다 |

## 지금 서 있는 자리 (2026-09-21)

| 화면 | 캔버스 | 코드 |
| --- | --- | --- |
| 프로젝트 목록 · 등록 | 없음 | 없음 |
| 작업 목록 · 간트 · 칸반 · 상세 · 추가 | 있음 | **없음** |
| 안건 목록 · 상세(이력) | 있음 | `/threads` + 팝업 |
| 회의 목록 | 있음(새로 그림) | `/meetings` — 열 · 칩이 새 시안과 다름 |
| 회의 상세 | 있음(새로 그림) | 팝업만 있음 — 맵핑 없음 |
| 새 회의 | 있음 | `/meetings/new` |
| 요청(`/requests`) | 옛 시안 | 있음 — **걷어낼 대상** |

그 밖에 어긋난 것

- `AppShell` 대메뉴가 `안건 · 회의 · 요청 · 일정관리`다. memo는 `작업 · 안건 · 회의` 셋.
- 프로젝트 전환이 장식이다. 스토어에 프로젝트가 `결제 안정화` 하나로 박혀 있고 데이터가 안 갈린다.
- 목업 데이터가 캔버스(한화손보 차세대 · memo 시나리오)와 딴 세상이다.
- 팝업에 URL이 없어 새로고침 · 뒤로가기가 안 산다.
- 테스트 · 린트 · API 레이어가 없다.

---

## 0단계 · 판 고르기

**목표** — 앞으로의 모든 작업이 빨라지는 것만 먼저 치운다.

- [x] **리포를 WSL(`~/workspace/mijeonge-project`)로 옮겼다.** `/mnt/c`(drvfs)는 inotify가 없다.
      `vite.config.ts`에 폴링 설정이 있어 HMR 자체는 돌고 있었지만, 옮기니
      `npm install` 14s → 3.4s, vite 기동 2955ms → 218ms, 폴링 CPU 부담도 사라졌다.
      폴링 설정은 걷어냈다. `/mnt/c` 사본은 지우지 않고 남겨 뒀다
- [x] eslint(flat config) + prettier — `npm run lint` · `npm run format`
- [x] vitest — `npm run test`. 첫 테스트는 `src/lib/date.test.ts`
- [x] 제품명 — `index.html` 타이틀, `package.json` 이름, `README.md`. `stores/mijeonge.ts`는
      1단계에서 쪼개면서 이름을 버린다
- [x] Vite 6 유지 (7로 올리는 건 지금 이득이 없다. 1~2단계 끝나고 다시 본다)
- [ ] **prettier 일괄 적용은 안 했다** — 지금 규칙과 어긋나는 14개 파일 대부분이 1~2단계에서
      다시 쓰인다. 파일을 건드릴 때 그때그때 맞춘다. 그래서 `format:check`는 아직 CI 조건이 아니다

**끝난 것으로 보는 기준** — 파일을 고치면 브라우저가 즉시 바뀌고, `lint` · `test` · `typecheck` 셋이 돈다.

**커밋** — `개발 판을 고른다 — lint · vitest · 이름`

## 1단계 · 모델과 껍데기

**목표** — 작업(task)이 들어올 자리를 만든다. 화면부터 만들면 두 번 고치게 된다.

### 1-1 도메인 타입

`src/types/domain.ts` 에 Task 를 더한다.

```ts
export type TaskStatus = 'todo' | 'doing' | 'blocked' | 'done'

export interface Task {
  id: string
  projectId: string
  parentId: string | null   // 계층. 목록은 평평하게 보여주고 경로만 회색 글씨로 적는다
  key: string               // HW-4
  title: string
  body: TaskLine[]          // 체크박스 · 불릿 줄
  status: TaskStatus
  ownerId: string | null
  start: string | null      // null 둘 = 기간 미정 → 간트에 막대 없음(unscheduled)
  due: string | null
  priority: 'low' | 'normal' | 'high'
  createdAt: string
}

export interface TaskLine {
  id: string
  kind: 'check' | 'bullet'
  text: string
  done: boolean
  level: number
}
```

### 1-2 맵핑은 링크로

```ts
export interface TaskThreadLink { taskId: string; threadId: string }
export interface MeetingTaskLink { meetingId: string; taskId: string }
```

안건 ↔ 회의는 이미 `Entry` 하나가 (회의 × 안건) 한 줄이라 그대로 둔다.
memo의 "안건 상세에서는 맵핑 불가"는 **화면 권한**이지 모델 제약이 아니다 — 링크는 양방향으로
두고 안건 화면에서만 읽기 전용으로 렌더한다.

### 1-3 스토어 쪼개기

`stores/mijeonge.ts`(641줄) 하나를 도메인별로 나눈다.

| 새 파일 | 담는 것 |
| --- | --- |
| `stores/data.ts` | 레코드가 사는 곳 + 프로젝트 · 멤버. 도메인 스토어끼리 서로를 부르면 고리가 생겨서 레코드를 한곳에 모았다 |
| `stores/task.ts` | 작업, 작업 행 계산, 상태 변경, 링크 |
| `stores/thread.ts` | 안건 · Entry · 안건 이력 계산 |
| `stores/meeting.ts` | 회의 · 회의 행 · 회의 상세 · 새 회의 저장 |
| `fixtures/*.ts` | 목업 데이터. **캔버스와 같은 한화손보 차세대 + memo 시나리오 4개로 교체** |

### 1-4 껍데기와 라우트

- [x] `AppShell` 대메뉴를 **작업 · 안건 · 회의**로. 프로젝트 선택이 실제로 스토어를 갈아끼운다
- [x] 라우트 재편 — 팝업도 URL을 갖는다(`/threads/:id` · `/meetings/:id`)

| 경로 | 화면 |
| --- | --- |
| `/projects` | 프로젝트 목록 · 등록 (4단계에서 채움) |
| `/tasks?view=list\|gantt\|board` | 작업 세 탭 |
| `/tasks/:id` | 작업 상세 (목록 위 팝업) |
| `/tasks/new` | 작업 추가 (목록 위 팝업) |
| `/threads`, `/threads/:id` | 안건 목록 · 이력 |
| `/meetings`, `/meetings/:id` | 회의 목록 · 상세 |
| `/meetings/new` | 새 회의 |

- [x] **`/requests` 제거** — 뷰 · 다이얼로그 · 배지 · 타입 · 라우트까지 걷어냈다
- [x] 옛 `stores/mijeonge.ts` 삭제. 화면은 새 스토어(data · thread · meeting · task)를 쓴다
- [ ] 기본 경로는 아직 `/threads` 다. 2단계에서 작업 목록이 서면 `/tasks` 로 바꾼다
- [ ] `TasksView.vue` 는 자리만 잡아 둔 화면이다 — 2-a 에서 진짜 목록으로 갈아끼운다

**끝난 것으로 보는 기준** — 작업 화면은 비어 있어도, 대메뉴 셋이 서고 프로젝트를 바꾸면
안건 · 회의 목록의 내용이 갈린다.

**커밋** — ① 모델과 스토어 분리 ② 셸 · 라우트 재편과 요청 걷어내기

## 2단계 · 작업 화면 넷

가장 큰 덩어리. 캔버스가 이미 부품을 떼어 놨으니(`TaskCreateForm` · `TaskDetailPanel`)
코드도 그 구조 그대로 간다 — 세 탭이 같은 부품을 가져다 쓴다.

### 2-a 작업 목록 (기본 탭)

- [x] 평평한 게시판. 계층을 펼치지 않고 한 줄에 하나씩
- [x] 열: 번호 · 작업 · 상태(줄에서 바로 바꾼다) · 담당자 · 기간 · 연관 안건
- [x] 제목 아래 회색 경로(`개발 › AI Biz › 가상비서`), 최상위면 `최상위 작업`, 자식 있으면 `하위 n건`
- [x] 조회 조건(제목 · 담당자) + 상태 칩(전체 · 해야 할 일 · 진행 중 · 완료 · 막힘 · **기간 미정**)
- [x] 페이징, 행 일괄 선택(담당자 · 상태 바꾸기)
- [x] 왼쪽 '눈여겨볼 것' 둘 — 기간 미정 / 막힘으로 바로 거는 버튼
- [x] 기본 경로를 `/tasks` 로 옮겼다
- [x] 화면이 실제로 뜨는지 마운트 테스트로 덮었다(`TasksView.test.ts`) — 브라우저 없이
      템플릿 오류와 거르기 동작을 잡는다. `@vue/test-utils` + jsdom
- [ ] 행을 눌러 상세로, 오른쪽 위 '작업 추가' — 둘 다 2-b 에서 붙인다 (지금은 눌리지 않는다)

### 2-b 작업 추가 · 작업 상세 (부품 둘)

- [ ] `TaskCreateForm` — 상위 작업 · 제목 · 내용(`[]` 체크박스, `-` 불릿) · 기간(미정 체크) ·
      상태 · 우선순위 · 담당자 · 연관 안건
- [ ] `TaskDetailPanel` — 2단(왼쪽 본문 · 오른쪽 필드)
  - 내용: 체크박스는 눌러서 켜고 끈다
  - 하위 작업: 진척 막대 + 표에서 바로 상태 변경
  - **연관 안건 맵핑** — 등록된 안건에서 고르기 · 연결 해제
  - **연관 회의 맵핑** — 캔버스에 아직 없다. 여기서 새로 붙인다(회의 상세의 맵핑과 같은 링크)
  - 활동: 댓글 · 상태 변경 기록
- [ ] 세 탭 어디서든 같은 팝업이 뜬다

### 2-c 칸반보드

- [ ] 칸 넷: 해야 할 일 · 진행 중 · **막힘** · 완료
- [ ] 카드의 상태 셀렉트로 칸 이동(끌어놓기는 뒤로 미룬다)
- [ ] 묶음 작업은 카드가 되지 않는다. 잎 작업만 올라온다
- [ ] 막힌 카드는 연관 안건 배지가 강조된다 — 눌러서 안건으로 간다

### 2-d 간트차트 (dhtmlx-gantt v10 Community)

- [ ] 스파이크 먼저: Vue 3 + Vite + TS 에 물리고 트리 그리드 · 막대 끌기 · 기간 미정까지 확인
- [ ] 래퍼 방식 — `onMounted` 에서 `gantt.init(el)`, 스토어 → `gantt.parse`,
      `gantt.attachEvent('onAfterTaskDrag'…)` 로 받아 스토어에 되돌리는 **단방향 고리**
- [ ] 기간 미정은 `unscheduled` 작업으로 (막대 없이 그리드에만 선다)
- [ ] 줄을 누르면 작업 상세 팝업. 접기 화살표와 `+` 는 팝업을 열지 않는다
- [ ] 주 / 개월 눈금, 오늘 세로선, 주말 회색 띠
- [ ] **라이선스 확인** — Community 는 GPL 계열. 사내 배포 전에 확답 받기

**끝난 것으로 보는 기준** — memo 시나리오 1·2를 세 탭에서 끝까지 따라갈 수 있다.
개발 환경 설정 작업을 만들고, 안건 둘을 걸고, 막힘으로 세우고, 기간 미정을 목록에서 챙긴다.

**커밋** — 2-a / 2-b / 2-c / 2-d 각각 하나씩

## 3단계 · API 계약과 `src/api/`

작업 화면이 끝나면 스키마에서 제일 무거운 것(작업 · 링크 · 기간)이 확정된다. 이때 붙인다.

- [ ] FastAPI 쪽과 **OpenAPI 스키마 합의** — 프로젝트 · 작업 · 안건 · 회의 · Entry · 링크
- [ ] `openapi-typescript` 로 타입 생성, `types/domain.ts` 와 맞춘다
- [ ] `src/api/` — fetch 래퍼 + 도메인별 클라이언트
- [ ] 스토어가 목업 배열 대신 api 를 부르게 바꾼다 (README 에 적어 둔 대로 "스토어만 바뀐다")
- [ ] `VITE_USE_MOCK` 스위치 — 서버가 없어도 화면 작업이 멈추지 않게
- [ ] 로딩 · 에러 · 빈 상태를 이때 한 번에 넣는다

## 4단계 · 회의 · 안건을 새 시안에 맞추기

- [ ] 회의 목록 열 교체 — 날짜 · 회의 · 참석자 · 남긴 줄 · 메모 · 연관 작업
- [ ] 종류 칩 교체 — 전체 · 안건 있음 · **메모만** · **미룸 남음**
      (지금의 `결정 남김 / 결정 없이 끝남` 칩을 대체)
- [ ] 회의 상세를 팝업(`MeetingDetailDialog`)에서 **전체 화면 라우트로 승격**
- [ ] 회의 상세 오른쪽 360 맵핑 패널 — 연관 안건(줄 붙은 것은 잠금) · 연관 작업
- [ ] 안건 이력에 **연관 작업**을 읽기 전용으로 추가 (시나리오 3에서 되돌아오는 길)
- [ ] 새 회의는 유지하되 저장 뒤 회의 상세로 보낸다

## 5단계 · 프로젝트 목록 · 등록

- [ ] `/projects` — 최상위 프로젝트를 만들고 고른다
- [ ] 고른 뒤 모든 화면이 그 프로젝트 안에서만 돈다 (1단계에서 스토어는 이미 갈려 있다)
- [ ] 빈 프로젝트 상태(작업 0 · 안건 0 · 회의 0)가 말이 되게

## 6단계 · 품질

- [ ] vitest — 스토어 계산: 안건 이력 정렬 · `superseded` · 집계 · 기간 미정 판정 · 작업 경로
- [ ] 키보드 · 포커스 · 대비
- [ ] 빈 상태와 에러 상태를 화면마다 확인

---

## 진행 상황

| 단계 | 상태 |
| --- | --- |
| 0 판 고르기 | **끝남** (2026-09-21) |
| 1 모델과 껍데기 | **끝남** (2026-09-21) |
| 2 작업 화면 넷 | 진행 중 — 2-a 목록 끝남 (2026-09-22) |
| 3 API 계약 | 시작 전 |
| 4 회의 · 안건 정렬 | 시작 전 |
| 5 프로젝트 | 시작 전 |
| 6 품질 | 시작 전 |

## 아직 안 정한 것

- 작업 상태 넷(해야 할 일 · 진행 중 · **막힘** · 완료)이 memo 에 없다. 막힘 칸은 캔버스에서
  덧붙인 것이라 memo 에 적어 확정할지 정해야 한다
- 회의 상세에서 줄이 붙은 안건도 뺄 수 있게 할지 (지금 시안은 잠가 뒀다)
- 작업을 회의에 걸 때 그 작업의 막힘을 회의 화면에서 바로 풀어줄지
- 요청(`/requests`)을 걷어내면 "회의 없이 담당자 확인만으로 처리하는 자잘한 일"은 안건의
  '회의 밖' 줄로만 남는다. 그것으로 충분한지
- dhtmlx-gantt Community 라이선스 확인 (사내 배포 기준)
